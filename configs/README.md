# Configuration

Versioned YAML configuration will define data snapshots, cohort rules, feature cutoffs, simulator assumptions, policy parameters, reward weights, constraints, and seed sets.

No headline result may depend on an uncommitted default.
